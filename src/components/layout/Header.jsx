import React from 'react';

const Header = () => {
  return (<header>
    <section className='container mx-auto'><b className='text-2xl'>React / redux</b></section>
  </header>);
};

export default Header;
